{
    "functions": {
        "api/*.js": {
            "runtime": "@vercel/node"
        }
    },
    "routes": [
        {
            "source": "/api/(.*)",
            "destination": "/server.js"
        }
    ]
}
