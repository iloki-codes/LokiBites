{
    "functions": {
        "api/*.js": {
            "runtime": "@vercel/node"
        }
    },
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/server.js"
        }
    ]
}
