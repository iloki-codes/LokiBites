{
    "functions": {
        "api/*.js": {
            "runtime": "@vercel/nodejs18.x"
        }
    },
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/server.js"
        }
    ]
}
