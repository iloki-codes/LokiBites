{
    "builds": [
        {
            "source": "./server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "source": "/api/(.*)",
            "destination": "/server.js"
        }
    ]
}
